import React from "react";
import "./SessionInProgress.scss";

const SessionInProgress = () => {
  return <section className="session_progress">progress</section>;
};

export default SessionInProgress;
